# Observability Feature

`/runs/[runId]/telemetry` provides runtime-facing observability surfaces:

- trace correlation and span drill-down
- worker health and runtime backlog health
- notification delivery records
- runtime SLA snapshot with scoped recovery/delivery trend
